<p id="notice"><%= notice %></p>

<h1>Blogs</h1>

<table>
  <thead>
    <tr>
      <th>Title</th>
      <th>Body</th>
      <th colspan="4"></th>
    </tr>
  </thead>

  <tbody>
  <%# rails is looks for a partial named 'blog'. 
  the instance vairable has to be passed in from the index action.
  the partial will render a collection without an each action.
  rail is looking for the model controller name, which in this case is blogs %>
    <%= render @blogs %> 
  </tbody>
</table>

<br>

<%= link_to 'New Blog', new_blog_path %>
