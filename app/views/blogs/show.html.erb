<div class="col-sm-8 blog-main">
  <h2> <%= @blog.title %> </h2>
  <%= link_to 'Edit', edit_blog_path(@blog) if logged_in?(:site_admin)%>
  <p> <%= @blog.body %> </p>

  <!-- start of comment form. Don't show if guest user-->
  <%= render 'comments/comment_form' %>
  <!-- end of comment form-->

  <!-- start of comments. iterate over -->
  <%= render @blog.comments %>

  <!-- end of comments-->
</div>

